<%= form_for(email_sign_up) do |f| %>
  <% if email_sign_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(email_sign_up.errors.count, "error") %> prohibited this email_sign_up from being saved:</h2>

      <ul>
      <% email_sign_up.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :email %>
    <%= f.text_field :email %>
  </div>

  <div class="field">
    <%= f.label :notification %>
    <%= f.check_box :notification %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
